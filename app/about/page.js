// Next js relies on reserved, special file name
// page.js-Defines pages content
// layout.js-defines wrapper around pages
// not-found.js-define not found callback page
// error-define error fallback page

const AboutPage = () => {
  return (
    <main>
      <h1>hello, About Us </h1>
    </main>
  );
};

export default AboutPage;
